-- temp location for my installed plugins until I find a better home for them

return {
  {
    -- https://github.com/numToStr/Comment.nvim
    -- Not using <leader>/ , using gcc instead
    "numToStr/Comment.nvim",
    opts = {
      -- add any options here
    },
    lazy = false,
  },
}
